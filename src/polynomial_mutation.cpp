#include "polynomial_mutation.h"
void NoMutation::operator()(double *population_genes, const double * min_value, const double * max_value, int population_size, int genes_number, const int * is_mutation_applicable)
{
    ;
}
